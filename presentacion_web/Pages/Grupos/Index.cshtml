@page
@model presentacion_web.Pages.Grupos.IndexModel
@{
    ViewData["Title"] = "Grupos";
}
<input type="hidden" id="jwtToken" value="@HttpContext.Session.GetString("JWToken")" />
<div class="container-fluid py-4">
    <div class="mb-4">
        <h2 class="fw-bold text-white mb-0"><i class="fas fa-users-cog me-2"></i>Grupos</h2>
        <p class="text-secondary mb-0">Grupos de hábitos colaborativos</p>
    </div>
    <div class="row g-3" id="listaGrupos"><div class="text-secondary">Cargando...</div></div>
</div>
@section Scripts {
    <script>
        const token = document.getElementById('jwtToken').value;
        async function cargar() {
            const c = document.getElementById("listaGrupos");
            try {
                const res = await fetch("http://localhost:5165/Grupos/Listar", { headers: { "Authorization": "Bearer " + token } });
                const data = await res.json();
                c.innerHTML = data.map(g => `
                    <div class="col-md-4">
                        <div class="card bg-dark border-secondary text-white h-100">
                            <div class="card-body">
                                <h5 class="fw-bold"><i class="fas fa-users me-2 text-info"></i>${g.nombre}</h5>
                                <p class="text-secondary mb-2">${g.descripcion ?? '—'}</p>
                                <small class="text-secondary">Administrador ID: ${g.administrador}</small>
                            </div>
                        </div>
                    </div>`).join('');
            } catch(e) { c.innerHTML = '<div class="text-danger">Error al cargar.</div>'; }
        }
        cargar();
    </script>
}